This folder will contain Firestore Security Rules unit tests (Firebase Emulator Suite).

Recommended structure:
- `firestore.rules_tests/index.js` (test runner)
- `firestore.rules_tests/specs/*.js` (individual rule cases)

Currently, only documentation is included in this repo.

