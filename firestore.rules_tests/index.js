/*
Firestore Security Rules unit test harness.

Run with:
  firebase emulators:exec "node firestore.rules_tests/index.js"

Note: Requires Firebase Emulator Suite + authenticated context.

This repo currently includes the harness stub only; no test cases are executed by default.
*/

console.log('Firestore rules test harness stub: add spec files in this folder.');

